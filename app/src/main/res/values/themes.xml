<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MeetWM" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/secondary_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorAccent">@color/light_green</item>
        <item name="colorSecondaryVariant">@color/green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    
    <style name="Theme.MeetWM.fullscreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="myMessageStyle">
        <item name="neumorph_cornerFamily">rounded</item>

        <item name="neumorph_cornerSizeTopLeft">14dp</item>
        <item name="neumorph_cornerSizeBottomLeft">14dp</item>
        <item name="neumorph_cornerSizeBottomRight">14dp</item>
    </style>
    <style name="otherMessageStyle">
        <item name="neumorph_cornerFamily">rounded</item>

        <item name="neumorph_cornerSizeTopRight">14dp</item>
        <item name="neumorph_cornerSizeBottomLeft">14dp</item>
        <item name="neumorph_cornerSizeBottomRight">14dp</item>
    </style>
</resources>